<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bigdata.dao.newStudentInfoDao">
    <select id="getAllStudent" resultType="com.bigdata.entity.NewStudent">
        SELECT * FROM newstudentinfo
    </select>

    <select id="getAllStudent1" resultType="int">
        SELECT score FROM newstudentinfo where name = "jack"
    </select>
</mapper>